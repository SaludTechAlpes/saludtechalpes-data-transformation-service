apiVersion: v1
kind: Secret
metadata:
  name: data-transformation-secrets
stringData:
  PHRASE_KEY: "{{PHRASE_KEY}}"
  DB_USER: "{{DB_USER}}"
  DB_PASSWORD: "{{DB_PASSWORD}}"
  DB_NAME: "{{DB_NAME}}"
  DB_PORT: "{{DB_PORT}}"
  DB_HOST: "{{DB_HOST}}"